Use this reference for archive and public-dataset follow-up questions.

- Prefer simple archive-appropriate keywords, modality labels, task labels, anatomy terms, or known study names.
- When the user cites a Zenodo DOI or record URL, prefer `get_zenodo_record`; use `search_zenodo_records` only if you need to recover or disambiguate deposits.
- Inspect returned dataset IDs or repository names before trying a second search path.
- Keep the lookup bounded to the user's follow-up. If the question turns into open-ended discovery across archives, defer to a formal investigation.

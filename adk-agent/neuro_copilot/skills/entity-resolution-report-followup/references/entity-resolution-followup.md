Use this reference for report follow-up about aliases and canonical identifiers.

- Use `resolve_gene_identifiers` for genes and aliases.
- Use `map_ontology_terms_oxo` for ontology and disease-term mapping.
- Use `search_hpo_terms` for phenotype normalization.
- Keep the answer focused on the specific ambiguous term in the report instead of reopening the investigation.
